# Homebrew tap for the BurnedSecret CLI

    brew install JensrudJ/burnedsecret/bs

Formulae are rendered and pushed by CI on each `cli-v*` release. Install docs: https://burnedsecret.com/install/cli/
